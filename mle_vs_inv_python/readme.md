### Instructions

Install the requirements using `pip install -r requirements`.

Run the Python jupyter notebook `MLE_RR_Experiments.ipynb`.

The file `estimators.py` contains the estimators defined in the paper, including especially `MLE`.
